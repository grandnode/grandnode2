@using System.Text.Json
@model string
@{
    var area = ViewContext.RouteData.Values["area"]?.ToString();
    var options = JsonSerializer.Serialize(new {
        url = Url.Action("Category", "Search", new { area, categoryId = Model }),
        placeholder = "Select category...",
        textField = "Name",
        valueField = "Id",
        filter = "startswith"
    });
}
@Html.TextBoxFor(model => model, new { @class = "col-md-8", style = "width: 100%;", data_grand_select = options })
