@model DateTime?
<input id="@Html.IdForModel()" name="@Html.NameForModel()" value="@(Model.HasValue ? Model.Value : "")"
       data-grand-date="datetime"
       data-grand-date-value="@(Model.HasValue ? Model.Value.ToString("yyyy-MM-ddTHH:mm:ss", CultureInfo.InvariantCulture) : "")"/>
