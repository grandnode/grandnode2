@using Grand.Web.AdminShared.Models.Common
@using Microsoft.AspNetCore.Mvc.Razor
@model AddressAttributeValueModel
<div asp-validation-summary="All"></div>
<input asp-for="Id" type="hidden"/>
<input asp-for="AddressAttributeId" type="hidden"/>

<partial name="Partials/WidgetZone.Value.Top" model="Model"/>
@{
    Func<int, HelperResult>
        template = @<div class="grand-fields">
                       <admin-field asp-for="@Model.Locales[item].Name"/>
                       <input type="hidden" asp-for="@Model.Locales[item].LanguageId"/>
                   </div>;
}

@* the checkout attribute value popup's layout: the localized name, the rest in two columns, and
   Save as the popup's one primary action *@
<localized-editor localized-template="@template" name="addressattributevalue-localized" language-ids="@Model.Locales.Select(c => c.LanguageId).ToList()">
    <div class="grand-fields">
        <admin-field asp-for="Name"/>
    </div>
</localized-editor>
<div class="grand-fields grand-fields--2col">
    <admin-field asp-for="DisplayOrder" size="sm"/>
    <admin-field asp-for="IsPreSelected"/>
</div>
<partial name="Partials/WidgetZone.Value.Bottom" model="Model"/>
<div class="grand-popup__actions">
    <button class="btn btn-primary" type="submit" name="save">
        <i class="bi bi-check-lg"></i> @Loc["Admin.Common.Save"]
    </button>
</div>
