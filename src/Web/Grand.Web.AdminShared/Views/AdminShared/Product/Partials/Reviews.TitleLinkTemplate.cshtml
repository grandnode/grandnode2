@{
    var area = ViewContext.RouteData.Values["area"]?.ToString();
}
@*content of the title cell template of the product reviews grid*@
<a class="k-link" href="@Url.Action("Edit", "ProductReview", new { area = area })/{{ Id }}">{{ Title }}</a>
