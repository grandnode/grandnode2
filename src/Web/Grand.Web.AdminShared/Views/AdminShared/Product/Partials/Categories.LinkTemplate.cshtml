@{
    var area = ViewContext.RouteData.Values["area"]?.ToString();
}
@*content of the CategoryId cell template of the product categories grid*@
<a class="text-decoration-none" href="@Url.Action("Edit", "Category", new { area = area })/{{ CategoryId }}">{{ Category }}</a>
