@{
    var area = ViewContext.RouteData.Values["area"]?.ToString();
}
@*content of the customer cell template of the product reviews grid*@
<a class="k-link" href="@Url.Action("Edit", "Customer", new { area = area })/{{ CustomerId }}">{{ CustomerInfo }}</a>
