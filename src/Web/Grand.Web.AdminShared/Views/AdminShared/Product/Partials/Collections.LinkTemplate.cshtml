@{
    var area = ViewContext.RouteData.Values["area"]?.ToString();
}
@*content of the CollectionId cell template of the product collections grid*@
<a class="k-link" href="@Url.Action("Edit", "Collection", new { area = area })/{{ CollectionId }}">{{ Collection }}</a>
