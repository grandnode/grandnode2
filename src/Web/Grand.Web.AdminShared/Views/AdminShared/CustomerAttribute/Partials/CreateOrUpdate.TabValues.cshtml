@model CustomerAttributeModel
<partial name="Partials/WidgetZone.Values.Top" model="Model"/>
@{
    var isReadOnly = Model.IsGlobalAttribute;
    var area = ViewContext.RouteData.Values["area"]?.ToString();

    if (!string.IsNullOrEmpty(Model.Id))
    {
        <div class="panel panel-default">
            <div class="panel-body">
                <admin-grid id="customerattributevalues-grid"
                            read-url="@Url.Action("ValueList", "CustomerAttribute", new { customerAttributeId = Model.Id, area })"
                            destroy-url="@(isReadOnly ? null : Url.Action("ValueDelete", "CustomerAttribute", new { area }))"
                            pager="Compact"
                            confirm-destroy="true"
                            on-data-bound="@(isReadOnly ? null : "bindCustomerAttributeValuePopups")">
                    <grid-column field="Name" title="@Loc["Admin.Customers.CustomerAttributes.Values.Fields.Name"]">
                        <cell-template>
                            @if (!isReadOnly)
                            {
                                <a class="k-link editvalue" href="@Url.Action("ValueEditPopup", "CustomerAttribute", new { area })/{{ Id }}?customerAttributeId=@Model.Id&btnId=btnRefresh&formId=customerattribute-form">{{ Name }}</a>
                            }
                            else
                            {
                                @:{{ Name }}
                            }
                        </cell-template>
                    </grid-column>
                    <grid-column field="IsPreSelected" title="@Loc["Admin.Customers.CustomerAttributes.Values.Fields.IsPreSelected"]" width="100" align="Center" min-screen-width="500">
                        <cell-template>
                            <i data-if="IsPreSelected" class="bi bi-check-lg" aria-hidden="true" style="color:green"></i>
                            <i data-else class="bi bi-x-lg" aria-hidden="true" style="color:red"></i>
                        </cell-template>
                    </grid-column>
                    <grid-column field="DisplayOrder" title="@Loc["Admin.Customers.CustomerAttributes.Values.Fields.DisplayOrder"]" width="100" align="Center"/>
                    @if (!isReadOnly)
                    {
                        <grid-commands destroy="true" title="@Loc["Admin.Common.Delete"]" width="100"/>
                    }
                </admin-grid>
            </div>
            @if (!isReadOnly)
            {
                <div class="panel-footer">
                    <a id="btnAddNewValue" href="@(Url.Action("ValueCreatePopup", "CustomerAttribute", new { customerAttributeId = Model.Id, btnId = "btnRefresh", formId = "customerattribute-form", area }))" class="k-button"><i class="bi bi-plus-lg"></i>&nbsp;@Loc["Admin.Customers.CustomerAttributes.Values.AddNew"]</a>
                    <input type="submit" id="btnRefresh" style="display: none"/>
                </div>
            }
            else
            {
                <input type="submit" id="btnRefresh" style="display: none"/>
            }
        </div>

        if (!isReadOnly)
        {
            <script>
                $(document).ready(function () {
                    $('#btnAddNewValue').magnificPopup({
                        type: 'ajax',
                        settings: { cache: false, async: false },
                        midClick: true,
                        callbacks: {
                            parseAjax: function (mfpResponse) {
                                mfpResponse.data = $('<div></div>').html(mfpResponse.data);
                            },
                            ajaxContentAdded: function () {
                                $('.mfp-wrap').removeAttr('tabindex');
                            }
                        }
                    });

                    $('#btnRefresh').click(function () {
                        var grid = $("#customerattributevalues-grid").data('kendoGrid');
                        grid.dataSource.read();
                        return false;
                    });
                });
                function bindCustomerAttributeValuePopups() {
                    $('.editvalue').magnificPopup({
                        type: 'ajax',
                        settings: { cache: false, async: false },
                        midClick: true,
                        callbacks: {
                            parseAjax: function (mfpResponse) {
                                mfpResponse.data = $('<div></div>').html(mfpResponse.data);
                            },
                            ajaxContentAdded: function () {
                                $('.mfp-wrap').removeAttr('tabindex');
                            }
                        }
                    });
                }
            </script>
        }
    }
    else
    {
        <div class="note note-info">
            @Loc["Admin.Customers.CustomerAttributes.Values.SaveBeforeEdit"]
        </div>
    }
}
<partial name="Partials/WidgetZone.Values.Bottom" model="Model"/>
