@model CustomerAttributeModel
@{
    //page title
    ViewBag.Title = Loc["Admin.Customers.CustomerAttributes.AddNew"];
    var area = ViewContext.RouteData.Values["area"]?.ToString();
}
<form asp-area="@area" asp-controller="CustomerAttribute" asp-action="Create" method="post" id="customerattribute-form">

    @* the same shell as Edit, minus what an attribute that does not exist yet cannot offer *@
    <admin-page sticky
                title="@Loc["Admin.Customers.CustomerAttributes.AddNew"]"
                icon="bi-list-ol"
                back-url="@Url.Action("List")"
                back-text="@Loc["Admin.Customers.CustomerAttributes.BackToList"]">
        <page-actions>
            <button class="btn btn-outline-secondary" type="submit" name="save-continue">
                <i class="bi bi-check-circle"></i> @Loc["Admin.Common.SaveContinue"]
            </button>
            <button class="btn btn-primary" type="submit" name="save">
                <i class="bi bi-check-lg"></i> @Loc["Admin.Common.Save"]
            </button>
            <partial name="Partials/WidgetZone.DetailsButtons"/>
        </page-actions>

        <partial name="Partials/CreateOrUpdate" model="Model"/>
    </admin-page>
</form>
