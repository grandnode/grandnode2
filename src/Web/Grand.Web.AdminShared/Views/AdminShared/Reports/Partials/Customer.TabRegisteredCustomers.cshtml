@model CustomerReportsModel
@* the registered-customers grid comes from each panel's own view component; the card is shared *@
<admin-card flush>
    <partial name="Partials/CustomerReportRegisteredComponent"/>
</admin-card>
