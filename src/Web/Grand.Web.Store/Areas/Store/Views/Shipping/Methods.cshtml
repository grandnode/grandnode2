@{
    ViewBag.Title = Loc["Admin.Configuration.Shipping.Methods"];
    Layout = Constants.LayoutStore;
}

<div class="row">
    <div class="col-md-12">
        <div class="x_panel light form-fit">
            <div class="x_title">
                <div class="caption">
                    <i class="bi bi-card-list"></i>
                    @Loc["Admin.Configuration.Shipping.Methods"]
                </div>
                <div class="actions btn-group btn-group-devided">
                    <a href="@Url.Action("CreateMethod", "Shipping", new { area = Constants.AreaStore })" class="btn green">
                        <i class="bi bi-plus-lg"></i><span class="d-none d-sm-inline"> @Loc["Admin.Common.AddNew"]</span>
                    </a>
                </div>
            </div>
            <div class="x_content form">
                <div class="form-horizontal">
                    <div class="form-body">
                        <div class="x_content">
                            <admin-grid id="shippingmethod-grid"
                                        read-url="@Url.Action("MethodsListData", "Shipping", new { area = Constants.AreaStore })"
                                        pager="Compact">
                                <grid-column field="Name" title="@Loc["Admin.Configuration.Shipping.Methods.Fields.Name"]">
                                    <cell-template>
                                        <a class="k-link" href="@Url.Action("EditMethod", "Shipping", new { area = Constants.AreaStore })/{{ Id }}">{{ Name }}</a>
                                    </cell-template>
                                </grid-column>
                                <grid-column field="Description" title="@Loc["Admin.Configuration.Shipping.Methods.Fields.Description"]"/>
                                <grid-column field="DisplayOrder" title="@Loc["Admin.Configuration.Shipping.Methods.Fields.DisplayOrder"]" width="120"/>
                            </admin-grid>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

