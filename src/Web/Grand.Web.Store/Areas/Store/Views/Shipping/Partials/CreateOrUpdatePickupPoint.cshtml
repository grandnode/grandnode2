@model PickupPointModel

<div asp-validation-summary="All"></div>
<input asp-for="Id" type="hidden"/>

@* the point itself, where it is, and what it costs; the address keeps the shared Address editor template *@
<admin-card>
    <div class="grand-fields grand-fields--2col">
        <admin-field asp-for="Name"/>
        <admin-field asp-for="Description"/>
        <admin-field asp-for="AdminComment">
            <admin-textarea asp-for="AdminComment"></admin-textarea>
        </admin-field>
    </div>
</admin-card>
<admin-card>
    <admin-input asp-for="Address" asp-template="Address"/>
</admin-card>
<admin-card>
    <div class="grand-fields grand-fields--2col">
        <admin-field asp-for="WarehouseId" asp-label-for="AvailableWarehouses" asp-items="Model.AvailableWarehouses"/>
        <admin-field asp-for="PickupFee" size="md"/>
        <admin-field asp-for="Latitude" size="md"/>
        <admin-field asp-for="Longitude" size="md"/>
        <admin-field asp-for="DisplayOrder" size="sm"/>
    </div>
</admin-card>
