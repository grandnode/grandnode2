@model ShippingMethodModel
@{
    ViewBag.Title = Loc["Admin.Configuration.Shipping.Methods.EditMethodDetails"];
    Layout = Constants.LayoutStore;
}
<form asp-area="@Constants.AreaStore" asp-controller="Shipping" asp-action="EditMethod" method="post">

    @* the record name as the title, Save as the one primary action, Delete in the overflow menu *@
    <admin-page sticky
                title="@Model.Name"
                icon="bi-card-list"
                meta="@Loc["Admin.Configuration.Shipping.Methods.EditMethodDetails"]"
                back-url="@Url.Action("Methods", "Shipping", new { area = Constants.AreaStore })"
                back-text="@Loc["Admin.Configuration.Shipping.Methods.BackToList"]">
        <page-actions>
            <button class="btn btn-outline-secondary" type="submit" name="save-continue">
                <i class="bi bi-check-circle"></i> @Loc["Admin.Common.SaveContinue"]
            </button>
            <button class="btn btn-primary" type="submit" name="save">
                <i class="bi bi-check-lg"></i> @Loc["Admin.Common.Save"]
            </button>
            <admin-action-menu>
                <admin-action id="method-delete" destructive="true" icon="bi-trash"
                              text="@Loc["Admin.Common.Delete"]"/>
            </admin-action-menu>
        </page-actions>

        <partial name="Partials/CreateOrUpdateMethod" model="Model"/>
    </admin-page>
</form>
<admin-delete-confirmation button-id="method-delete" action-name="DeleteMethod"/>
