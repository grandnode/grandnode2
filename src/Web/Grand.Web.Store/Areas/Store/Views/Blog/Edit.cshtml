@model BlogPostModel
@{
    //page title
    ViewBag.Title = Loc["Admin.Content.Blog.BlogPosts.EditBlogPostDetails"];
}

<form asp-area="@Grand.Web.Store.Extensions.Constants.AreaStore" asp-controller="Blog" asp-action="Edit" method="post" id="blog-edit">

    <div class="row">
        <div class="col-md-12">
            <div class="x_panel light form-fit">
                <div class="x_title">
                    <div class="caption">
                        <i class="fa fa-comment"></i>
                        @Loc["Admin.Content.Blog.BlogPosts.EditBlogPostDetails"] - @Model.Title
                        <small>
                            <i class="fa fa-arrow-circle-left"></i>@Html.ActionLink(Loc["Admin.Content.Blog.BlogPosts.BackToList"], "List")
                        </small>
                    </div>
                    <div class="actions">
                        <div class="btn-group btn-group-devided">
                            <button type="button" onclick="window.open('@Url.RouteUrl("BlogPost", new { Model.SeName })','_blank');" class="btn purple">
                                <i class="fa fa-eye"></i>
                                @Loc["Admin.Common.Preview"]
                            </button>
                            <button class="btn btn-success" type="submit" name="save">
                                <i class="fa fa-check"></i> @Loc["Admin.Common.Save"]
                            </button>
                            <button class="btn btn-success" type="submit" name="save-continue">
                                <i class="fa fa-check-circle"></i> @Loc["Admin.Common.SaveContinue"]
                            </button>
                            <span id="blogpost-delete" class="btn red">
                                <i class="fa fa-trash-o"></i> @Loc["Admin.Common.Delete"]
                            </span>
                        </div>
                    </div>
                </div>
                <div class="x_content form">
                    <partial name="Partials/CreateOrUpdate" model="Model"/>
                </div>
            </div>
        </div>
    </div>
</form>

<script>
    $(document).ready(function() {
        $('#blogpost-delete').click(function() {
            if (confirm('@Loc["Admin.Common.AreYouSure"]')) {
                var postData = {
                    id: '@Model.Id'
                };
                addAntiForgeryToken(postData);
                
                $.ajax({
                    cache: false,
                    type: "POST",
                    url: "@(Url.Action("Delete", "Blog", new { area = Grand.Web.Store.Extensions.Constants.AreaStore }))",
                    data: postData,
                    success: function() {
                        location.href = "@(Url.Action("List", "Blog", new { area = Grand.Web.Store.Extensions.Constants.AreaStore }))";
                    },
                    error: function(xhr, ajaxOptions, thrownError) {
                        alert('Failed to delete');
                    }
                });
            }
            return false;
        });
    });
</script>