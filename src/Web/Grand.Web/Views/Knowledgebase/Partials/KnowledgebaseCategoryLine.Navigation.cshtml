@model KnowledgebaseCategoryModel

<ul class="sublist">
    @if (Model.IsCurrent)
    {
        <li class="active last">
            <a href="@Url.RouteUrl("KnowledgebaseCategory", new { Model.SeName })"><strong>@Model.Name</strong></a>
            @foreach (var category in Model.Children)
            {
                <partial name="Partials/KnowledgebaseCategoryLine.Navigation" model="category" />
            }
        </li>
    }
    else
    {
        <li class="inactive">
            <a href="@Url.RouteUrl("KnowledgebaseCategory", new { Model.SeName })">@Model.Name</a>
        </li>
    }
</ul>