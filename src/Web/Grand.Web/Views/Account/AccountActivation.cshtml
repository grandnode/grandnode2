@model AccountActivationModel
@inject IPageHeadBuilder pagebuilder
@{
    Layout = "_SingleColumn";

    //title
    pagebuilder.AddTitleParts(Loc["Title.AccountActivation"]);
}
<div class="page account-activation-page">
    <h1 class="h2 generalTitle">@Loc["Account.AccountActivation"]</h1>
    <div class="result">
        <div class="result alert alert-info">@Model.Result</div>
    </div>
</div>
