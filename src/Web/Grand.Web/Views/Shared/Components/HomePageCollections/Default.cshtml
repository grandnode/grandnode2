@model IList<CollectionModel>
@inject DisplayOrderHomePage order
<div class="categoryGrid  @order.HomePageCollections">
    <div cols="12" class="col-12 px-0">
        <div class="row">
            @foreach (var item in Model)
            {
                <div class="col-xl-@order.Pm_Xl col-lg-@order.Pm_Lg col-md-@order.Pm_Md col-sm-@order.Pm_Sm col-@order.Pm_Col">
                    <div class="card picture-card mb-2">
                        <a href="@Url.RouteUrl("Collection", new { item.SeName })" title="@item.PictureModel.Title">
                            <img class="card-img" src="@item.PictureModel.ImageUrl" alt="@item.PictureModel.AlternateText" loading="lazy">
                        </a>
                    </div>
                </div>
            }
        </div>
    </div>
</div>