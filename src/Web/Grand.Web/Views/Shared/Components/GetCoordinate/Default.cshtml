@model LocationModel
<script type="application/json" data-grand-vm="geolocation">@Json.Serialize(new {
                                                                route = Url.RouteUrl("CurrentPosition"),
                                                                known = new { latitude = Model.Latitude, longitude = Model.Longitude }
                                                            })</script>
