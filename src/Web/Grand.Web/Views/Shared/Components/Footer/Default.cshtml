@model FooterModel

<partial name="_NewsletterBox"/>

<footer class="mt-3">
    <b-container>
        <b-form-row>
            <b-col id="footer-block-information" cols="12" class="footer-block information block">
                <div class="step-title card mb-1 p-3 d-lg-none">
                    <span class="h5 text-center mb-0"><strong>@Loc["Footer.Information"]</strong></span>
                </div>
                <div class="viewBox">
                    <b-list-group>
                        @if (Model.SitemapEnabled)
                        {
                            <b-list-group-item href="@Url.RouteUrl("Sitemap")">@Loc["Sitemap"]</b-list-group-item>
                        }
                        @if (Model.PrivacyPreference)
                        {
                            <b-list-group-item id="btnPrivacyPreference" href="#" onclick="GetPrivacyPreference('@Url.RouteUrl("PrivacyPreference")'); return false;">@Loc["PrivacyPreference"]</b-list-group-item>
                        }
                        @foreach (var mpage in Model.Pages.Where(x => x.IncludeInFooterRow1).ToList())
                        {
                            <b-list-group-item href="@Url.RouteUrl("Page", new { SeName = mpage.SeName })">@mpage.Name</b-list-group-item>
                        }
                        <b-list-group-item href="@Url.RouteUrl("ContactUs")">@Loc["ContactUs"]</b-list-group-item>
                        <b-list-group-item href="@Url.RouteUrl("ProductSearch")">@Loc["Search"]</b-list-group-item>
                        @if (Model.NewsEnabled)
                        {
                            <b-list-group-item href="@Url.RouteUrl("NewsArchive")">@Loc["News"]</b-list-group-item>
                        }
                        @if (Model.BlogEnabled)
                        {
                            <b-list-group-item href="@Url.RouteUrl("Blog")">@Loc["Blog"]</b-list-group-item>
                        }
                        @if (Model.KnowledgebaseEnabled)
                        {
                            <b-list-group-item href="@Url.RouteUrl("Knowledgebase")">@Loc["Knowledgebase"]</b-list-group-item>
                        }
                        @if (Model.RecentlyViewedProductsEnabled)
                        {
                            <b-list-group-item href="@Url.RouteUrl("RecentlyViewedProducts")">@Loc["Products.RecentlyViewedProducts"]</b-list-group-item>
                        }
                        @if (Model.CompareProductsEnabled)
                        {
                            <b-list-group-item href="@Url.RouteUrl("CompareProducts")">@Loc["Products.Compare.List"]</b-list-group-item>
                        }
                        @if (Model.NewProductsEnabled)
                        {
                            <b-list-group-item href="@Url.RouteUrl("NewProducts")">@Loc["Products.NewProducts"]</b-list-group-item>
                        }
                    </b-list-group>
                </div>
            </b-col>
            <b-col id="footer-block-my-account" cols="12" class="footer-block my-account block">
                <div class="step-title card mb-1 p-3 d-lg-none">
                    <span class="h5 text-center mb-0"><strong>@Loc["Footer.MyAccount"]</strong></span>
                </div>
                <div class="viewBox">
                    <b-list-group class="pl-0 list-group list-pad">
                        <b-list-group-item href="@Url.RouteUrl("CustomerInfo")">@Loc["Account.MyAccount"]</b-list-group-item>
                        <b-list-group-item href="@Url.RouteUrl("CustomerOrders")">@Loc["Account.CustomerOrders"]</b-list-group-item>
                        <b-list-group-item href="@Url.RouteUrl("CustomerAddresses")">@Loc["Account.CustomerAddresses"]</b-list-group-item>
                        @if (Model.ShoppingCartEnabled)
                        {
                            <b-list-group-item href="@Url.RouteUrl("ShoppingCart")">@Loc["ShoppingCart"]</b-list-group-item>
                        }
                        @if (Model.WishlistEnabled)
                        {
                            <b-list-group-item href="@Url.RouteUrl("Wishlist")">@Loc["Wishlist"]</b-list-group-item>
                        }
                        @if (Model.AllowCustomersToApplyForVendorAccount)
                        {
                            <b-list-group-item href="@Url.RouteUrl("ApplyVendorAccount")">@Loc["Vendors.ApplyAccount"]</b-list-group-item>
                        }
                        @foreach (var mpage in Model.Pages.Where(x => x.IncludeInFooterRow2).ToList())
                        {
                            <b-list-group-item href="@Url.RouteUrl("Page", new { SeName = mpage.SeName })" target="_blank">@mpage.Name</b-list-group-item>
                        }
                    </b-list-group>
                </div>
                <div class="viewBox">
                    <b-list-group class="pl-0 list-group list-pad">
                        @foreach (var mpage in Model.Pages.Where(x => x.IncludeInFooterRow3).ToList())
                        {
                            <b-list-group-item class="list-group-item" href="@Url.RouteUrl("Page", new { SeName = mpage.SeName })">@mpage.Name</b-list-group-item>
                        }
                    </b-list-group>
                </div>
            </b-col>
            <b-col id="footer-block-about-us" cols="12" class="footer-block about-us block">
                <div class="step-title card mb-1 p-3 d-lg-none">
                    <span class="h5 text-center mb-0"><strong>@Loc["Footer.AboutUs"]</strong></span>
                </div>
                <div class="viewBox">
                    <b-list-group class="bg-light my-3">
                        @if (!string.IsNullOrEmpty(Model.CompanyName))
                        {
                            <b-list-group-item>
                                <span>@Loc["Footer.CompanyName.Title"]</span>
                                <span>@Model.CompanyName</span>
                            </b-list-group-item>
                        }
                        @if (!string.IsNullOrEmpty(Model.CompanyAddress))
                        {
                            <b-list-group-item>
                                <span>@Loc["Footer.Address.Title"]</span>
                                <span>@Model.CompanyAddress</span>
                            </b-list-group-item>
                        }
                        @if (!string.IsNullOrEmpty(Model.CompanyEmail))
                        {
                            <b-list-group-item>
                                <span>@Loc["footer.Email.Title"]</span>
                                <span>@Model.CompanyEmail</span>
                            </b-list-group-item>
                        }
                        @if (!string.IsNullOrEmpty(Model.CompanyPhone))
                        {
                            <b-list-group-item>
                                <span>@Loc["Footer.Phone.Title"]</span>
                                <span>@Model.CompanyPhone</span>
                            </b-list-group-item>
                        }
                        @if (!string.IsNullOrEmpty(Model.CompanyHours))
                        {
                            <b-list-group-item>
                                <span>@Loc["Footer.Hour.Title"]</span>
                                <span>@Model.CompanyHours</span>
                            </b-list-group-item>
                        }
                        </b-list-group>
                    </div>
                </b-col>
                <ol class="d-flex flex-row list-group justify-content-center w-100 flex-wrap social-icons list-pad pb-0 mb-3">
                    @if (!string.IsNullOrEmpty(Model.FacebookLink))
                    {
                        <li class="list-group-item facebook">
                            <a title="facebook" href="@Model.FacebookLink" rel="noopener" target="_blank">
                                <img height="32" width="32" src="/theme/images/facebook.svg" alt="facebook" />
                                <span class="sr-only">Facebook</span>

                            </a>
                        </li>
                    }
                    @if (!string.IsNullOrEmpty(Model.TwitterLink))
                    {
                        <li class="list-group-item twitter">
                            <a title="twitter" href="@Model.TwitterLink" rel="noopener" target="_blank">
                                <img height="32" width="32" src="/theme/images/twitter.svg" alt="twitter" />
                                <span class="sr-only">twitter</span>
                            </a>
                        </li>
                    }
                    @if (!string.IsNullOrEmpty(Model.YoutubeLink))
                    {
                        <li class="list-group-item youtube">
                            <a title="youtube" href="@Model.YoutubeLink" rel="noopener" target="_blank">
                                <img height="32" width="32" src="/theme/images/youtube.svg" alt="youtube" />
                                <span class="sr-only">youtube</span>
                            </a>
                        </li>
                    }
                    @if (!string.IsNullOrEmpty(Model.InstagramLink))
                    {
                        <li class="list-group-item instagram">
                            <a href="@Model.InstagramLink" rel="noopener" target="_blank">
                                <img height="32" width="32" src="/theme/images/instagram.svg" alt="instagram" />
                                <span class="sr-only">instagram</span>
                            </a>
                        </li>
                    }
                    @if (!string.IsNullOrEmpty(Model.LinkedInLink))
                    {
                        <li class="list-group-item linkedin">
                            <a href="@Model.LinkedInLink" rel="noopener" target="_blank">
                                <img height="32" width="32" src="/theme/images/linkedin.svg" alt="linkedin" />
                                <span class="sr-only">linkedin</span>
                            </a>
                        </li>
                    }
                    @if (!string.IsNullOrEmpty(Model.PinterestLink))
                    {
                        <li class="list-group-item pinterest">
                            <a href="@Model.PinterestLink" rel="noopener" target="_blank">
                                <img height="32" width="32" src="/theme/images/pinterest.svg" alt="pinterest" />
                                <span class="sr-only">pinterest</span>
                            </a>
                        </li>
                    }
                </ol>
            </b-form-row>
            <div class="footer-lower small py-3 border-top">
                <b-row>
                    <b-col md="6" cols="12" class="pb-1 pt-1 flex-column text-lg-left text-center copyright">
                        <span class="footer-disclaimer">@Html.Raw(Loc["Content.CopyrightNotice", DateTime.Now.Year, Model.StoreName])</span>
                    </b-col>
                    <b-col md="6" cols="12" class="pb-1 pt-1 text-center text-lg-right">
                        <span>Shopping cart software by </span><a class="pr-1 text-success" href="https://grandnode.com/">GrandNode</a>™
                    </b-col>
                </b-row>
            </div>
        </b-container>
        @await Component.InvokeAsync("Widget", new { widgetZone = "footer" })
    </footer>
