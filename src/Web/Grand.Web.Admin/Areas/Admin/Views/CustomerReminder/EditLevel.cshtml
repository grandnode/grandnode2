@model CustomerReminderModel.ReminderLevelModel
@{
    //page title
    ViewBag.Title = Loc["Admin.Customers.CustomerReminder.EditLevel"];
}

<form asp-area="@Constants.AreaAdmin" asp-controller="CustomerReminder" 
      asp-action="EditLevel" 
      asp-route-CustomerReminderId="@Context.Request.Query["CustomerReminderId"]"
      asp-route-cid="@Context.Request.Query["cid"]"
      method="post" 
      id="customer-reminder-level-form">
    
    <div class="row">
        <div class="col-md-12">
            <div class="x_panel light form-fit">
                <div class="x_title">
                    <div class="caption">
                        <i class="fa fa-sitemap"></i>
                        @Loc["Admin.Customers.CustomerReminder.EditLevel"] - @Model.Name
                        <small>@Html.ActionLink("(" + Loc["Admin.Customers.CustomerReminder.BackToAction"] + ")", "Edit", new { Id = Model.CustomerReminderId })</small>
                    </div>
                    <div class="actions">
                        <div class="btn-group btn-group-devided">
                            <button class="btn btn-success" type="submit" name="save"><i class="fa fa-check"></i> @Loc["Admin.Common.Save"] </button>
                            <button class="btn btn-success" type="submit" name="save-continue"><i class="fa fa-check-circle"></i> @Loc["Admin.Common.SaveContinue"] </button>
                        </div>
                    </div>
                </div>
                <div class="x_content form">
                   <partial name="_CreateOrUpdateLevel" model="Model" />
                </div>
            </div>
        </div>
    </div>
</form>