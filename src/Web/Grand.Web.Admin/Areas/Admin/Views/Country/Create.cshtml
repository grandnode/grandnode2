@model CountryModel
@{
    //page title
    ViewBag.Title = Loc["Admin.Configuration.Countries.AddNew"];
}
<form asp-area="@Constants.AreaAdmin" asp-controller="Country" asp-action="Create" method="post" id="country-form">

    @* the same shell as Edit, minus what a record that does not exist yet cannot offer *@
    <admin-page sticky
                title="@Loc["Admin.Configuration.Countries.AddNew"]"
                icon="bi-globe"
                back-url="@Url.Action("List", "Country", new { area = Constants.AreaAdmin })"
                back-text="@Loc["Admin.Configuration.Countries.BackToList"]">
        <page-actions>
            <button class="btn btn-outline-secondary" type="submit" name="save-continue">
                <i class="bi bi-check-circle"></i> @Loc["Admin.Common.SaveContinue"]
            </button>
            <button class="btn btn-primary" type="submit" name="save">
                <i class="bi bi-check-lg"></i> @Loc["Admin.Common.Save"]
            </button>
            <vc:admin-widget widget-zone="country_details_buttons" additional-data="null"/>
        </page-actions>

        <partial name="Partials/CreateOrUpdate" model="Model"/>
    </admin-page>
</form>
