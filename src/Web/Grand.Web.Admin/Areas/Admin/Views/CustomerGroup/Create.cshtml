@model CustomerGroupModel
@{
    //page title
    ViewBag.Title = Loc["Admin.Customers.CustomerGroups.AddNew"];
}
<form asp-area="@Constants.AreaAdmin" asp-controller="CustomerGroup" asp-action="Create" method="post" id="customergroup-form">
    
    <div class="row">
        <div class="col-md-12">
            <div class="x_panel light form-fit">
                <div class="x_title">
                    <div class="caption">
                        <i class="fa fa-users"></i>
                        @Loc["Admin.Customers.CustomerGroups.AddNew"]
                        <small><i class="fa fa-arrow-circle-left"></i>@Html.ActionLink(Loc["Admin.Customers.CustomerGroups.BackToList"], "List")</small>
                    </div>
                    <div class="actions">
                        <button class="btn btn-success" type="submit" name="save"><i class="fa fa-check"></i> @Loc["Admin.Common.Save"] </button>
                        <button class="btn btn-success" type="submit" name="save-continue"><i class="fa fa-check-circle"></i> @Loc["Admin.Common.SaveContinue"] </button>
                        <vc:admin-widget widget-zone="customer_role_details_buttons" additional-data="null" />
                    </div>
                </div>
                <div class="x_content form">
                   <partial name="_CreateOrUpdate" model="Model" />
                </div>
            </div>
        </div>
    </div>
</form>