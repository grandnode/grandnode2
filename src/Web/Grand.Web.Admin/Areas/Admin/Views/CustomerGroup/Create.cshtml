@model CustomerGroupModel
@{
    //page title
    ViewBag.Title = Loc["Admin.Customers.CustomerGroups.AddNew"];
}
<form asp-area="@Constants.AreaAdmin" asp-controller="CustomerGroup" asp-action="Create" method="post" id="customergroup-form">

    @* the same shell as Edit, minus what a group that does not exist yet cannot offer *@
    <admin-page sticky
                title="@Loc["Admin.Customers.CustomerGroups.AddNew"]"
                icon="bi-people"
                back-url="@Url.Action("List")"
                back-text="@Loc["Admin.Customers.CustomerGroups.BackToList"]">
        <page-actions>
            <button class="btn btn-outline-secondary" type="submit" name="save-continue">
                <i class="bi bi-check-circle"></i> @Loc["Admin.Common.SaveContinue"]
            </button>
            <button class="btn btn-primary" type="submit" name="save">
                <i class="bi bi-check-lg"></i> @Loc["Admin.Common.Save"]
            </button>
            <vc:admin-widget widget-zone="customer_group_details_buttons" additional-data="null"/>
        </page-actions>

        <partial name="Partials/CreateOrUpdate" model="Model"/>
    </admin-page>
</form>
