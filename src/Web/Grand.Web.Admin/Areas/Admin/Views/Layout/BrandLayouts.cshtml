@{
    //page title
    ViewBag.Title = Loc["Admin.Configuration.Layouts.Brand"];
}

@* the grid edits inline; its Add new moves into the card header (grid.js) *@
<admin-page title="@Loc["Admin.Configuration.Layouts.Brand"]" icon="bi-list-task">
    <page-actions>
        <vc:admin-widget widget-zone="brand_layout_list_buttons" additional-data="null"/>
    </page-actions>

    <admin-card flush>
        <admin-grid id="layout-grid"
                    read-url="@Url.Action("BrandLayouts", "Layout", new { area = Constants.AreaAdmin })"
                    create-url="@Url.Action("BrandLayoutAdd", "Layout", new { area = Constants.AreaAdmin })"
                    update-url="@Url.Action("BrandLayoutUpdate", "Layout", new { area = Constants.AreaAdmin })"
                    destroy-url="@Url.Action("BrandLayoutDelete", "Layout", new { area = Constants.AreaAdmin })"
                    pager="Compact"
                    edit-mode="Inline">
            <grid-toolbar-create/>
            <grid-column field="Name" title="@Loc["Admin.Configuration.Layouts.Brand.Name"]" width="300" editor="Text"/>
            <grid-column field="ViewPath" title="@Loc["Admin.Configuration.Layouts.Brand.ViewPath"]" width="300" editor="Text"/>
            <grid-column field="DisplayOrder" title="@Loc["Admin.Configuration.Layouts.Brand.DisplayOrder"]" width="100" format="{0:0}" editor="Integer" default-value="0"/>
            <grid-commands edit="true" destroy="true" width="200"/>
        </admin-grid>
    </admin-card>
</admin-page>
