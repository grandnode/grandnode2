@model KnowledgebaseCategoryModel
@{
    //page title
    ViewBag.Title = Loc["Admin.Content.Knowledgebase.KnowgledgebaseCategory.CreateKnowgledgebaseCategory"];
}
<form asp-area="@Constants.AreaAdmin" asp-controller="Knowledgebase" asp-action="CreateCategory" method="post">

    @* the same shell as EditCategory, minus what a category that does not exist yet cannot offer *@
    <admin-page sticky
                title="@Loc["Admin.Content.Knowledgebase.KnowgledgebaseCategory.CreateKnowgledgebaseCategory"]"
                icon="bi-chat-square-text"
                back-url="@Url.Action("List")"
                back-text="@Loc["Admin.Content.Knowledgebase.KnowgledgebaseCategory.BackToList"]">
        <page-actions>
            <button class="btn btn-outline-secondary" type="submit" name="save-continue">
                <i class="bi bi-check-circle"></i> @Loc["Admin.Common.SaveContinue"]
            </button>
            <button class="btn btn-primary" type="submit" name="save">
                <i class="bi bi-check-lg"></i> @Loc["Admin.Common.Save"]
            </button>
        </page-actions>

        <partial name="Partials/CreateOrUpdateCategory" model="Model"/>
    </admin-page>
</form>
