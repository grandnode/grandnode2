@model KnowledgebaseArticleModel
@{
    //page title
    ViewBag.Title = Loc["Admin.Content.Knowledgebase.KnowgledgebaseArticle.CreateKnowgledgebaseArticle"];
}
<form asp-area="@Constants.AreaAdmin" asp-controller="Knowledgebase" asp-action="CreateArticle" method="post">

    <div class="row">
        <div class="col-md-12">
            <div class="x_panel light form-fit">
                <div class="x_title">
                    <div class="caption">
                        <i class="bi bi-chat-square-text"></i>
                        @Loc["Admin.Content.Knowledgebase.KnowgledgebaseArticle.CreateKnowgledgebaseArticle"]
                        <small>
                            <i class="bi bi-arrow-left-circle"></i> @Html.ActionLink(Loc["Admin.Content.Knowledgebase.KnowgledgebaseArticle.BackToList"], "List")
                        </small>
                    </div>
                    <div class="actions">
                        <div class="btn-group btn-group-devided">
                            <button class="btn btn-success" type="submit" name="save">
                                <i class="bi bi-check-lg"></i> @Loc["Admin.Common.Save"]
                            </button>
                            <button class="btn btn-success" type="submit" name="save-continue">
                                <i class="bi bi-check-circle"></i> @Loc["Admin.Common.SaveContinue"]
                            </button>
                        </div>
                    </div>
                </div>
                <div class="x_content form">
                    <partial name="Partials/CreateOrUpdateArticle" model="Model"/>
                </div>
            </div>
        </div>
    </div>
</form>