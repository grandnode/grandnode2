@model CampaignModel
@{
    //page title
    ViewBag.Title = Loc["admin.marketing.Campaigns.AddNew"];
}
<form asp-area="@Constants.AreaAdmin" asp-controller="Campaign" asp-action="Create" method="post" id="campaign-form">

    @* the same shell as Edit, minus what a campaign that does not exist yet cannot offer *@
    <admin-page sticky
                title="@Loc["admin.marketing.Campaigns.AddNew"]"
                icon="bi-envelope"
                back-url="@Url.Action("List")"
                back-text="@Loc["admin.marketing.Campaigns.BackToList"]">
        <page-actions>
            <button class="btn btn-outline-secondary" type="submit" name="save-continue">
                <i class="bi bi-check-circle"></i> @Loc["Admin.Common.SaveContinue"]
            </button>
            <button class="btn btn-primary" type="submit" name="save">
                <i class="bi bi-check-lg"></i> @Loc["Admin.Common.Save"]
            </button>
            <vc:admin-widget widget-zone="campaign_details_buttons" additional-data="null"/>
        </page-actions>

        <partial name="Partials/CreateOrUpdate" model="Model"/>
    </admin-page>
</form>
