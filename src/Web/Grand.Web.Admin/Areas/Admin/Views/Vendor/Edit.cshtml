@model VendorModel
@{
    //page title
    ViewBag.Title = Loc["Admin.Vendors.EditVendorDetails"];
}
<form asp-area="@Constants.AreaAdmin" asp-controller="Vendor" asp-action="Edit" method="post">

    @* the vendor's name as the title, Save as the one primary action; activate or deactivate beside it and Delete in the overflow menu, each asking the panel modal first *@
    <admin-page sticky
                title="@Model.Name"
                icon="bi-box"
                meta="@Loc["Admin.Vendors.EditVendorDetails"]"
                back-url="@Url.Action("List")"
                back-text="@Loc["Admin.Vendors.BackToList"]">
        <page-actions>
            @* activate / deactivate is a button of the header, not a menu entry: it is the vendor's state, checked often *@
            @if (Model.Active)
            {
                <button class="btn btn-outline-secondary" type="submit" name="activatevendor" value="deactive"
                        formaction="@Url.Action("Activate", new { id = Model.Id, area = Constants.AreaAdmin })"
                        data-grand-confirm="@Loc["Admin.Common.AreYouSure"]"
                        data-confirm-title="@Loc["Admin.Common.Deactivate"]">
                    <i class="bi bi-pause-circle"></i> @Loc["Admin.Common.Deactivate"]
                </button>
            }
            else
            {
                <button class="btn btn-outline-secondary" type="submit" name="activatevendor" value="active"
                        formaction="@Url.Action("Activate", new { id = Model.Id, area = Constants.AreaAdmin })"
                        data-grand-confirm="@Loc["Admin.Common.AreYouSure"]"
                        data-confirm-title="@Loc["Admin.Common.Activate"]">
                    <i class="bi bi-play-circle"></i> @Loc["Admin.Common.Activate"]
                </button>
            }
            <button class="btn btn-outline-secondary" type="submit" name="save-continue">
                <i class="bi bi-check-circle"></i> @Loc["Admin.Common.SaveContinue"]
            </button>
            <button class="btn btn-primary" type="submit" name="save">
                <i class="bi bi-check-lg"></i> @Loc["Admin.Common.Save"]
            </button>
            <admin-action-menu>
                <admin-action id="vendor-delete" destructive="true" icon="bi-trash"
                              text="@Loc["Admin.Common.Delete"]"/>
            </admin-action-menu>
            <vc:admin-widget widget-zone="vendor_details_buttons" additional-data="Model"/>
        </page-actions>

        <partial name="Partials/CreateOrUpdate" model="Model"/>
    </admin-page>
</form>
<admin-delete-confirmation button-id="vendor-delete"/>