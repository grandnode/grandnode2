@model CustomerModel
@{
    //page title
    ViewBag.Title = Loc["Admin.Customers.Customers.AddNew"];
}
<form asp-area="@Constants.AreaAdmin" asp-controller="Customer" asp-action="Create" method="post">

    @* the same shell as Edit, minus what a customer that does not exist yet cannot offer *@
    <admin-page sticky
                title="@Loc["Admin.Customers.Customers.AddNew"]"
                icon="bi-person"
                back-url="@Url.Action("List")"
                back-text="@Loc["Admin.Customers.Customers.BackToList"]">
        <page-actions>
            <button class="btn btn-outline-secondary" type="submit" name="save-continue">
                <i class="bi bi-check-circle"></i> @Loc["Admin.Common.SaveContinue"]
            </button>
            <button class="btn btn-primary" type="submit" name="save">
                <i class="bi bi-check-lg"></i> @Loc["Admin.Common.Save"]
            </button>
            <vc:admin-widget widget-zone="customer_details_buttons" additional-data="null"/>
        </page-actions>

        <partial name="Partials/CreateOrUpdate" model="Model"/>
    </admin-page>
</form>