@model string
@{
    var random = CommonHelper.GenerateRandomInteger();
    //content saved before sanitization was applied on write is still in the database
    var editorValue = Html.RawSanitized(ViewData.TemplateInfo.FormattedModelValue as string).ToHtmlString();
}
<div id="s-@(random)-summernote" class="summernote" data-name="null" data-file="null" data-format="null">
    @Html.Raw(editorValue)
</div>
@Html.TextArea(string.Empty, /* Name suffix */
    editorValue /* Initial value */
)
<script>
    $(document).ready(function () {
        $('#s-@(random)-summernote').summernote({
            toolbar: [
                ['style', ['style']],
                ['font', ['bold', 'underline', 'clear']],
                ['fontname', ['fontname']],
                ['color', ['color']],
                ['para', ['ul', 'ol', 'paragraph']],
                ['table', ['table']],
                ['insert', ['link', 'image', 'hr']],
                ['view', ['fullscreen', 'codeview', 'help']],
            ],
            callbacks: {
                    onChange: function (contents) {
                            var textarea = this.nextSibling.nextElementSibling;
                            textarea.value = contents;
                        },
            },
        });
    });
</script>