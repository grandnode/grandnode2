@model VendorPortalModel

<div class="card">
    <div class="card-title">
        <div class="h3 text-center">
            @Model.Title
        </div>
    </div>
    <div class="card-body">
        @Html.Raw(Model.Body)
    </div>
</div>