@inject IContextAccessor contextAccessor
@{
    //page title
    ViewBag.Title = Loc["Vendor.Dashboard.Statistics"];
}

<div class="grand-dashboard">
    <header class="grand-dashboard__header">
        <div>
            <h1 class="grand-dashboard__title">@Loc["Vendor.Dashboard.Statistics"]</h1>
            <p class="grand-dashboard__subtitle">
                <i class="bi bi-person-badge" aria-hidden="true"></i>
                <span>@contextAccessor.WorkContext.CurrentVendor.Name</span>
            </p>
        </div>
    </header>

    <vc:vendor-widget widget-zone="vendor_dashboard_statistics_before"/>
    <div class="row g-3">
        <div class="col-12 col-xl-6">
            <section class="grand-card h-100" aria-labelledby="statistics-bestsellers-quantity">
                <header class="grand-card__header">
                    <i class="bi bi-trophy grand-card__icon" aria-hidden="true"></i>
                    <h2 id="statistics-bestsellers-quantity" class="grand-card__title">@Loc["Vendor.Reports.BestSellers.ByQuantity"]</h2>
                </header>
                <div class="grand-card__body">
                    <vc:vendor-bestsellers-brief-report-by-quantity></vc:vendor-bestsellers-brief-report-by-quantity>
                </div>
            </section>
        </div>
        <div class="col-12 col-xl-6">
            <section class="grand-card h-100" aria-labelledby="statistics-bestsellers-amount">
                <header class="grand-card__header">
                    <i class="bi bi-cash-coin grand-card__icon" aria-hidden="true"></i>
                    <h2 id="statistics-bestsellers-amount" class="grand-card__title">@Loc["Vendor.Reports.BestSellers.ByAmount"]</h2>
                </header>
                <div class="grand-card__body">
                    <vc:vendor-bestsellers-brief-report-by-amount></vc:vendor-bestsellers-brief-report-by-amount>
                </div>
            </section>
        </div>
    </div>
    <vc:vendor-widget widget-zone="vendor_dashboard_statistics_after"/>
</div>
