@{
    Layout = "_AdminLayout";
    ViewBag.Title = Loc["Payments.BrainTree.FriendlyName"];
}
@model Payments.BrainTree.Models.ConfigurationModel
<input id="active-menu-item" type="hidden" value="/Admin/Payment/Methods"/>
@* the store scope, then the mode switches, the credentials and the fee; Save as the one primary
   action in the page header instead of the _ConfigurePlugin chrome *@
<form asp-controller="PaymentBrainTree" asp-action="Configure" method="post">
    <admin-page title="@Loc["Payments.BrainTree.FriendlyName"]" icon="bi-credit-card">
        <page-actions>
            <button type="submit" name="save" class="btn btn-primary">
                <i class="bi bi-check-lg"></i> @Loc["Admin.Common.Save"]
            </button>
        </page-actions>

        @await Component.InvokeAsync("StoreScope")

        <admin-card>
            <div class="grand-fields grand-fields--2col">
                <admin-field asp-for="Use3DS"/>
                <admin-field asp-for="UseSandBox"/>
            </div>
        </admin-card>
        <admin-card>
            <div class="grand-fields grand-fields--2col">
                <admin-field asp-for="MerchantId"/>
                <admin-field asp-for="PublicKey"/>
                <admin-field asp-for="PrivateKey"/>
            </div>
        </admin-card>
        <admin-card>
            <div class="grand-fields grand-fields--2col">
                <admin-field asp-for="AdditionalFee" size="md"/>
                <admin-field asp-for="AdditionalFeePercentage"/>
                <admin-field asp-for="DisplayOrder" size="sm"/>
            </div>
        </admin-card>
    </admin-page>
</form>
